services:
  deps-tracker:
    build: .
    ports:
      - "4000:4000"
    env_file:
      - .env
    volumes:
      - scan-results:/app/results
      - notif-state:/app/data
    restart: unless-stopped

volumes:
  scan-results:
  notif-state:
